1. Run the git_repo_cloner to clone the target repositories.
2. Run the docker-compose-parser to parse the options from the Docker Compose files.
3. Run the git_repo_evolution_extractor to extract the evolutionary data from the rrepositories.

Note: All the scripts are written in pythpon. Please read the comments inside the scripts before running them and fix them 
with appropriate paths.